# OpenArm ROS2 × LeRobot 插件

完整流程（安装、版本切换、ROS 启动、采集）见：

**[/home/openarm/vla/LeRobot_OpenArm_启动指南.md](/home/openarm/vla/LeRobot_OpenArm_启动指南.md)**

本包是 LeRobot 侧插件（非 ROS 包），用 `pip install -e` 安装，不要放进 `ros2_arms/src` 做 colcon。
